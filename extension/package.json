{
  "name": "zoom-workflow-recorder",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "prebuild": "tsc -b ../packages/workflow-core",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zoom-automation/workflow-core": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.2"
  }
}
